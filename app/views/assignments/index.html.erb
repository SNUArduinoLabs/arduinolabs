
<h3>Assignments Issued so Far</h3>

<table class="table table-striped table-hover">
  <thead>
    <th>Title</th>
    <th>Description</th>
    <th>Level</th>
    <th>Difficulty</th>
    <th>Challenged</th>
  </thead>
  <tbody>
    <% @assignments.each do |asg| %>
      <tr>
        <td><%= link_to asg.title, assignment_path(asg) %></td>
        <td><%= asg.tagline %></td>
        <td><%= asg.level %></td>
        <td><%= asg.difficulty %></td>
        <td>
          <% if current_user.submissions.where("assignment_id is ?", asg.id).any? %>
            <span class="label label-success">Already Taken</span>
          <% else %>
            <span class="label label">Not Taken</span>
          <% end %>
        </td>
    <% end %>
  </tbody>
</table>






